"""Migration scaffolding for moving the browser layer from
undetected_chromedriver (Selenium, synchronous) to nodriver (async, CDP-native).

This package is a PARALLEL, self-contained prototype. It does not modify or
import the existing Selenium code. See migration/nodriver/ for the async
implementation and migration/nodriver/README.md for how to run it.
"""
