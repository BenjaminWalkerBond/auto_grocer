"""Integration tests: exercise the REAL HEB API / a live session.

Skipped by default (they need credentials/network and can trip the WAF). Run
explicitly with ``pytest --run-integration`` — see ``tests/conftest.py``.
"""
